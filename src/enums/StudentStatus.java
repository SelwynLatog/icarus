package enums;

// Enrollment status of a person entering campus.
// Determines campus access rights and whether violations are tracked.
public enum StudentStatus {
    ENROLLED,
    OUTSIDER,
    SUSPENDED
}